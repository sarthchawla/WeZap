<center>
    <% users.forEach(function(user) { %>
        <div class="card card-container">
            <div class="row">
                <div class="col-sm-2">
                    <img id="profile-img" align="left" class="profile-img-card" src="//ssl.gstatic.com/accounts/ui/avatar_2x.png" />
                </div>
                <div class="col-sm-10">
                    <h4>@
                        <%= post.username %>
                    </h4>
                    <h4>
                        <%= post.title %>
                    </h4>
                    <ul type="none">
                        <li>
                            <%= post.content %>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    <% }); %>
</center>